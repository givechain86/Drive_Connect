/**
 * Next evaluates this file for both Node and Edge. Do not import `path`, `fs`,
 * or `@next/env` here — that breaks the Edge bundle (`__import_unsupported`).
 * Server env loading lives in `lib/env-bootstrap.ts` (API routes + server only).
 */
export async function register() {
  // intentionally empty
}
